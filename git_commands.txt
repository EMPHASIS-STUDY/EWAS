###################
#basic git commands
###################

#clone repo
git clone https://github.com/asaffa/EMPHASIS.git

#get latest
git pull

#add new files (local)
git add -A .

#commit changes
git commit -a -m 'meffil latest & analysis plan & git commands'

#push changes to repo master branch (remote)
git push -u origin master
